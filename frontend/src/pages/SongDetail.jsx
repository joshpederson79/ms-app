export default function SongDetail() {
  return (
    <div>
      <h2>Song</h2>
      <p className="muted">TODO: metadata grid, ChordSheet, recording player, threaded comments, version history.</p>
    </div>
  );
}
